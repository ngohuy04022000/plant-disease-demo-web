# ==========================================
# FastAPI & Backend Server
# ==========================================
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# ==========================================
# TensorFlow & Keras (MobileNetV2 Pipeline)
# ==========================================
tensorflow>=2.10.0
h5py>=3.9.0

# ==========================================
# PyTorch & ViT Pipeline
# ==========================================
# Lưu ý: torch và torchvision nên được cài đặt qua lệnh riêng biệt 
# để khớp với phiên bản CUDA của hệ thống.
torch>=2.0.0
torchvision>=0.15.0
timm>=1.0.25

# ==========================================
# Image Processing & Augmentation
# ==========================================
albumentations>=2.0.0
opencv-python-headless>=4.8.0  # Cần thiết cho albumentations trên server/WSL
Pillow>=9.1.0                  # Bắt buộc >= 9.1.0 để dùng Image.Resampling.LANCZOS

# ==========================================
# Utilities & Data
# ==========================================
numpy<2.0.0                    # Giới hạn < 2.0.0 để tránh xung đột với các bản TF/PyTorch cũ
scikit-learn>=1.3.0            # Dùng cho compute_class_weight
tqdm>=4.65.0